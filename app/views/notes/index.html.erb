<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Notes</title>
</head>
<body>

	<div id="notes">
		<%= render @notes %>
	</div>

		<%= form_for @new_notes, remote: true do |f| %>
			<div class="row">
				<div class="small-7 large-4 columns left">
					Title: <%= f.text_field :title %>
				</div>
			</div>
			<div class="row">
				<div class="small-7 large-4 columns left">
					Comment: <%= f.text_field :comment %>
				</div>
			</div>
			<div class="row">
				<div class="small-7 large-4 columns left">
					<%= f.submit nil, :class => 'tiny button radius' %>
				</div>
			<div>
		<% end %>
</body>
